[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "vectra-artifacts"
version = "0.1.0"
requires-python = ">=3.10"

[tool.setuptools]
py-modules = ["compiler_config"]

[tool.setuptools.packages.find]
where = ["."]